# .cursorrules
> Copy this file to project root as `.cursorrules`
> Replace [BRACKETED] values with project specifics.

---

```
You are building the [PROJECT NAME] marketing site.

Always read CLAUDE.md before writing any code.
Always read tasks/lessons.md at the start of each session.
Follow the design system in app/globals.css.

Server Components by default.
Never add "use client" to pages or layouts.
All section content inside <Container>.
[THEME] theme only. CSS variable colors only.
Never hardcode hex colors.
Never use <img> — always next/image.
Never load fonts via <link> — only next/font in layout.tsx.
Mobile-first CSS. Base styles first, then breakpoints.

When in doubt: read CLAUDE.md.
When something breaks: stop, re-plan, fix root cause.
After any mistake: update tasks/lessons.md.
```
